import styles from '@/styles/components/landing.module.scss';

export default function Landing() {
    return (
        <div className='section'>
            <div className={styles.container}>
                <div className={`${styles.bg} ${styles.type}`}></div>
            </div>
        </div>
    )
}
